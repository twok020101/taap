# Required API keys — see docs/feat-live-openaq-gfw.md for minting instructions.

OPENAQ_API_KEY=
GFW_API_KEY=

# Server only. Add the real value to .env.local or the hosting environment.
TYPESAFE_API_KEY=
# Pin the evaluated model; rerun pnpm eval:jev before changing it.
TYPESAFE_MODEL=jev-1.13.0
